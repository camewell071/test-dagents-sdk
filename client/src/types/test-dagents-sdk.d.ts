declare module 'test-dagents-sdk' {
    // Example of declaring an exported function 'greet'
    export function greet(name: string): string;

    // Add more declarations as necessary based on your SDK
}